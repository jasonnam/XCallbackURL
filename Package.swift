// swift-tools-version:5.0

import PackageDescription

let package = Package(
    name: "XCallbackURL",
    products: [
        .library(
            name: "XCallbackURL",
            targets: ["XCallbackURL"])
    ],
    targets: [
        .target(
            name: "XCallbackURL",
            dependencies: [])
    ]
)
